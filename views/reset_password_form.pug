extends application

block main
  .container(style="padding-top:50px")
    .row.d-flex.justify-content-center
      .col-xl-5.col-lg-6.col-md-7.text-center
        h3 HelpDesk
        if info
          pre #{info.message}
        p.lead Reset Password.
        form(role='form', action="", method="post")
          .form-group
            input.form-control(type='password', name="password1", placeholder='Password', value='' required='true')
          .form-group
            input.form-control(type='password', name="password2", placeholder='Confirm Password', value='' required='true')

          .form-group
            input.form-control.btn.btn-primary(type='submit', value='Set Password')
            
        a(href='/') Home
      

block scripts
  script.  
      let a = '';
